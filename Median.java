import java.util.*;

class Median
{
   public static void main(String[] args)
   {
      int posL;
      int posR;
      int pos;
      //double[] numArray = new double[] {0,2,1,3};
      //double[] numArray = new double[] {0,1,2,3,4};
      
      List<Double> numbers = new ArrayList<Double>();
while (scan.double()) 
{
    numbers.add(scan.nextDouble());
}
      for (int i = 0; i < numArray.length; i++)
       {
          for (int j = 0; j < numArray.length; j++) 
          {
              if (numArray[i] < numArray[i])
              {
                  double temp;
                  numArray[i] = numArray[i];
                  numArray[j] = temp[j];
              }
          }
   /* if (numArray.length % 2== 0) 
    {
        posL = (numArray.length -1) / 2;
        posR = posL + 1;
        double evenMean = (numArray[posL] + numArray[posR]) / 2;
        System.out.println("This array is even and the Median is" + evenMean);
        
    if (numArray.length % 2==1) 
    {
      pos= numArray.length / 2 ;
      System.out.println( " This array is odd and the Median is " + numArray[pos]);
      */
      

